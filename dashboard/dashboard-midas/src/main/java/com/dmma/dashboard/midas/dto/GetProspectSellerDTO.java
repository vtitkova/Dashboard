package com.dmma.dashboard.midas.dto;


public class GetProspectSellerDTO {

    public String epost;

    /*public User toSeller() {
        if (!isValid()) {
            return null;
        }

        User seller = new User();
        seller.setEmail(epost);
        seller.createDefaultPassword();
        seller.addGroup(Group.SELLERS);
        return seller;
    }

    public boolean isValid() {
        return StringUtils.isNotEmpty(epost);
    }*/

}
